import "./Profile.css";

function Profile() {
  return (
    <div className="profile">
      <div className="profile__recipes">
        {/*when api is ready: take the array of favourites card and map it , returning recipeCard component */}
      </div>
      <div className="profile__grocery-list">
        {/* when api is ready take the array of scheduled meals and calculate all the ingredients 
      declare calculation function above return
      map the calculations to a new array
      map this array here */}
      </div>
    </div>
  );
}

export default Profile;
